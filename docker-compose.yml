services:
  processing:
    env_file:
      - .env
    image: ${ECR_REGISTRY}/tilda/processing:${ENVIRONMENT}-latest
    container_name: processing
    environment:
      PGHOST: ${DATABASE_HOST}
      PGDATABASE: ${DATABASE_NAME}
      PGUSER: ${DATABASE_USER}
      PGPASSWORD: ${DATABASE_PASSWORD}
      PROCESS_GEOFABRIK_DOWNLOAD_URL:
      PROCESS_GEOFABRIK_OAUTH_OSM_USERNAME:
      PROCESS_GEOFABRIK_OAUTH_OSM_PASSWORD:
      SKIP_UNCHANGED:
      SKIP_DOWNLOAD:
      WAIT_FOR_FRESH_DATA:
      SKIP_WARM_CACHE:
      PROCESS_ONLY_TOPICS:
      PROCESS_ONLY_BBOX:
      OSM2PGSQL_LOG_LEVEL:
      OSM2PGSQL_NUMBER_PROCESSES:
      TILES_URL:
      ENVIRONMENT:
      PROCESSING_DIFFING_MODE:
      PROCESSING_DIFFING_BBOX:
      ATLAS_API_KEY:
      FORCE_COLOR: 1
    volumes:
      - osmfiles:/data
      - /var/run/docker.sock:/var/run/docker.sock
      - ./app/src/data/processingTypes:/data/processingTypes
      # See cache_proxy/.README.md
      - ./cache_proxy:/cache_nginx_proxy
    depends_on:
      db:
        condition: service_healthy
  app:
    image: ${ECR_REGISTRY}/tilda/app:${ENVIRONMENT}-latest
    container_name: app
    restart: unless-stopped
    # TanStack Start (Vite + Nitro, Bun). Port 4000 = Nitro server.
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://127.0.0.1:4000/ || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s
    environment:
      PORT: 4000
      DATABASE_HOST: ${DATABASE_HOST}
      DATABASE_USER: ${DATABASE_USER}
      DATABASE_PASSWORD: ${DATABASE_PASSWORD}
      DATABASE_NAME: ${DATABASE_NAME}
      ENVIRONMENT:
      VITE_APP_ENV:
      VITE_APP_ORIGIN:
      APP_URL:
      CACHELESS_URL:
      TILES_URL:
      SESSION_SECRET_KEY:
      OSM_CLIENT_ID:
      OSM_CLIENT_SECRET:
      S3_KEY:
      S3_SECRET:
      S3_REGION:
      S3_BUCKET:
      ATLAS_API_KEY:
      MAPROULETTE_API_KEY:
      BREVO_API_KEY:
      # Bun closes idle HTTP connections after ~10s by default.
      # Large exports can take >10s before first response bytes are available.
      NITRO_BUN_IDLE_TIMEOUT: 120
      FORCE_COLOR: 1
    ports:
      - 4000:4000
    labels:
      traefik.enable: true
      traefik.http.services.container.loadbalancer.server.port: 4000
      # Apex host only (non-www canonical).
      traefik.http.routers.app-apex.rule: Host(`${APP_URL}`)
      traefik.http.routers.app-apex.entrypoints: websecure
      traefik.http.routers.app-apex.tls.certresolver: letsencrypt
      traefik.http.routers.app-apex.tls: true
      traefik.http.routers.app-apex.service: container
      # www → apex 301 at the edge (no app-side redirect).
      traefik.http.middlewares.www-to-apex.redirectregex.regex: "^https://www\\.(.+)$"
      traefik.http.middlewares.www-to-apex.redirectregex.replacement: "https://$$1"
      traefik.http.middlewares.www-to-apex.redirectregex.permanent: "true"
      traefik.http.routers.app-www.rule: Host(`www.${APP_URL}`)
      traefik.http.routers.app-www.entrypoints: websecure
      traefik.http.routers.app-www.tls.certresolver: letsencrypt
      traefik.http.routers.app-www.tls: true
      traefik.http.routers.app-www.middlewares: www-to-apex
      traefik.http.routers.app-www.service: container
    depends_on:
      db:
        condition: service_healthy
      tiles:
        condition: service_started
  tiles:
    image: ghcr.io/maplibre/martin:1.7.0
    container_name: tiles
    command: ["--config", "/config.yaml"]
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql://${DATABASE_USER}:${DATABASE_PASSWORD}@${DATABASE_HOST}/${DATABASE_NAME}
      # https://maplibre.org/martin/troubleshooting.html?highlight=log#troubleshooting
      # RUST_LOG: actix_web=info,martin=debug,tokio_postgres=debug
      RUST_LOG: actix_web=info,martin=info,tokio_postgres=info
      TZ: Europe/Berlin
    labels:
      traefik.enable: true
      traefik.http.routers.tiles.rule: Host(`${CACHELESS_URL}`)
      traefik.http.routers.tiles.entrypoints: websecure
      traefik.http.routers.tiles.tls.certresolver: letsencrypt
      traefik.http.routers.tiles.tls: true
    ports:
      - 3000:3000
    configs:
      - source: martin_conf
        target: /config.yaml
    depends_on:
      db:
        condition: service_healthy
  db:
    image: ghcr.io/baosystems/postgis:17-3.5
    container_name: db
    shm_size: 2g
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DATABASE_USER}
      POSTGRES_DB: ${DATABASE_NAME}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
      LANG: en_US.UTF-8
      LC_ALL: en_US.UTF-8
      TZ: Europe/Berlin
    ports:
      - "5432:5432"
    volumes:
      - db_postgres_17:/var/lib/postgresql/data
    configs:
      - source: postgres_conf
        target: /postgresql/postgresql.conf
      - source: postgres_init
        target: /docker-entrypoint-initdb.d/11-postgres.sql
    command: ["-c", "config_file=/postgresql/postgresql.conf"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DATABASE_USER} -d ${DATABASE_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  db_postgres_17:
  osmfiles:

configs:
  martin_conf:
    file: ./configs/martin.yaml
  postgres_init:
    file: ./configs/postgres-init.sql
  postgres_conf:
    file: ./configs/postgres.conf
